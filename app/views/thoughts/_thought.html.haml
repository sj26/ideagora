%article.thought
  = textilize(thought.description).try(:html_safe)
  -if thought.liked_by? current_user
    = link_to "unlike", thought_unlike_path(thought), :"data-action" => "like"
  - else
    = link_to "like", thought_like_path(thought), :"data-action" => "unlike"
  